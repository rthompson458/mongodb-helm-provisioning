apiVersion: mongodb.com/v1
kind: MongoDB

metadata:
  name: mongodb-poc
  namespace: mongodb

spec:
  type: ReplicaSet
  members: 3
  version: "8.0.0"

  opsManager:
    configMapRef:
      name: my-project

  credentials: organization-secret
